{
  "name": "example-playground",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Node + TypeScript demo of @anilkumarthakur/match, resolved through the built package (validates the real exports map and type declarations).",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "start": "node --experimental-strip-types src/main.ts"
  },
  "dependencies": {
    "@anilkumarthakur/match": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.0.9",
    "typescript": "^5.9.3"
  }
}
